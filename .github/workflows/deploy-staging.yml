name: Deploy Staging
on:
  push:
    branches: [ "staging" ]
  workflow_dispatch: {}

concurrency:
  group: staging-deploy-${{ github.repository }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Add VPS host key to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -t ed25519 72.167.41.196 >> ~/.ssh/known_hosts
      - name: Run site deploy script on VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 72.167.41.196
          username: deploy
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            sudo /usr/local/bin/deploy-saleem-net-staging
